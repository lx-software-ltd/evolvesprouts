# Public WWW Analytics Change Log

Use this log for every analytics-impacting Public WWW change.

## Required fields per entry

- Date (UTC)
- PR/branch reference
- Public WWW code changes
- GA4 updates (custom definitions/key events)
- GTM updates (workspace/container/version status)
- Documentation updates

## Entries

| Date (UTC) | Ref | Public WWW changes | GA4 updates | GTM updates | Documentation updates |
|---|---|---|---|---|---|
| 2026-09-22 | `cursor/mba-booking-share-link-b556` | My Best Auntie confirm-and-pay still opens from a cohort deep link (`booking_system`, `service_tier`, `cohort`, optional `ref` or `discount`). The public modal does not show a copy control, and `booking_share_link_copied` is not in the taxonomy. Admin Services instances can open the same link for MBA training cohorts (`admin_booking_link_opened`, `admin_booking_link_copied`, `admin_booking_link_qr_downloaded`). | **Manual:** No new custom dimensions. Do not add a tag for `booking_share_link_copied`. Admin events stay outside the public taxonomy. | **Manual:** Do not add a GTM trigger for `booking_share_link_copied`. | Runbook event catalog and admin event table, marketing-stack component table, this change log. |
| 2026-05-04 | `cursor/free-intro-call-booking-b152` | Free intro-call landing (`/book-a-free-call`): taxonomy adds `intro_call_slot_selected` (optional `slot_start`), `intro_call_slot_picker_status_change` (optional `status`), `book_free_call_click` (optional `cta_location`). Instrumentation in `intro-call-slot-picker.tsx` and `landing-page-free-intro-call.tsx`. Reuses `trackPublicFormOutcome` `booking_submit_*` with `service_tier: intro_call`, `cohort_date: ''`, `total_amount: 0`; Meta Pixel `Schedule`/`Purchase` + GA4 eCommerce `purchase` for zero-value intro call. Meta `content_name` for Pixel events: `public_www_free_intro_call` (matches taxonomy key; not the legacy hyphenated string). | **Manual:** Register optional event params `slot_start`, `status` if reporting on picker funnel; `cta_location` may already exist from other events. No new key events required for intro-call-only events. If Meta events were filtered on legacy `content_name` `public-www-free-intro-call`, update to `public_www_free_intro_call`. | **Manual:** Add/verify GTM custom-event triggers + GA4 tags for the three new events if funnels need them; wire `booking_submit_*` from intro-call section if not already covered by generic reservation tags. | `analytics-ga4-gtm-runbook.md` (scope + catalog + dimensions list), this change log, `marketing-stack.md` (Meta `content_name`). |
| 2026-04-27 | `cursor/age-group-to-service-tier-rename-e926` | Renamed booking analytics param `age_group` → `service_tier` (taxonomy + instrumentation). Renamed public reservation API fields: `childAgeGroup` → `serviceTier`, normalized payload `child_age_group` → `service_tier`, payment-intent `cohort_age` → `service_tier`, Stripe metadata `cohort_age` → `service_tier`. Thank-you recap locale key `detailAgeGroupLineTemplate` → `detailServiceTierLineTemplate` with placeholder `{serviceTier}` (visible copy unchanged). | **Manual:** Create event-scoped custom dimension `service_tier` (display name **Service Tier**). Stop using / retire reporting on legacy `age_group` for booking events after cutover. Existing PaymentIntents keep `metadata[cohort_age]`; new ones use `metadata[service_tier]`. | **Manual:** Update GTM variables and GA4 event tags so booking events map `service_tier` (replacing `age_group`). | Runbook, marketing-stack, GA4 manual setup steps, `ga4-create-custom-dimensions.py`, this change log. |
| 2026-04-24 | `cursor/instances-table-remove-uuid-copy-3852` | Admin: removed Instances list “Copy instance UUID” control; `admin_instance_uuid_copied` no longer emitted from `admin-analytics.ts`. | If `admin_instance_uuid_copied` was registered for reporting only, no GA4 change required; otherwise retire unused event. | Remove or disable GTM mapping for `admin_instance_uuid_copied` if it was wired. | Runbook admin event table; this change log. |
| 2026-04-21 | PR #1303 / `cursor/free-booking-reservations-32c8` | Zero-total bookings: `reservation-form.tsx` sends `payment_method: 'free'` on `trackPublicFormOutcome` (`booking_submit_*`) and eCommerce `purchase` when the post-discount total is HK$0; `add_payment_info` is skipped for free. No new event names. | No new dimensions; `payment_method` may include `free`; `total_amount` may be `0` on success paths. | No change required; eCommerce `purchase` with value `0` is valid. | Runbook: free-booking semantics for `booking_submit_*` and `purchase`; this change log. |
| 2026-04-17 | `cursor/ads-performance-assessment-2026-04-17-17ed` | Widened `MetaPixelStandardEvent` union to include `AddPaymentInfo`, `Purchase`, and `CompleteRegistration`. Booking reservation success now fires `Purchase` (currency HKD, value = total) alongside `Schedule`. My Best Auntie Confirm-and-Pay CTA fires `AddPaymentInfo`. Community signup success fires `CompleteRegistration` alongside `Lead`. Taxonomy flipped `booking_confirm_pay_click.ga4KeyEvent` from `false` to `true`. Extended `meta-pixel-taxonomy.ts` to allow the new events on the relevant content_name entries. | **Applied via API** on 2026-04-17: created GA4 Key Events for `booking_confirm_pay_click` and `whatsapp_click`; promoted the auto-synced Google Ads `(web) booking_confirm_pay_click`, `(web) whatsapp_click`, `(web) media_form_submit_success`, and `(web) community_signup_submit_success` conversion actions to **ENABLED + Secondary** (see `apps/public_www/marketing/scripts/apply/ga4-mark-key-events.py` and `google-ads-enable-secondary-conversions.py`). No new custom dimensions. | No trigger rename; existing `booking_confirm_pay_click` custom-event trigger now flows as a key event in GA4. | Updated `analytics-ga4-gtm-runbook.md` (booking_confirm_pay_click key-event flag + step-5 key-event list). Added `apps/public_www/marketing/reports/google-ads-manual-setup-steps.md` and `meta-ads-manual-setup-steps.md`. Extended `ga4-manual-setup-steps.md` with the post-deploy Pixel verification checklist. |
| 2026-04-17 | `cursor/discount-referral-scope-qr-bb55` | Referral auto-apply: `reservation-form.tsx` fires **`booking_discount_autoapply_success`** / **`booking_discount_autoapply_error`** (taxonomy already defines params). Manual apply events unchanged. Admin follow-up: optional `NEXT_PUBLIC_ADMIN_GTM_CONTAINER_ID` + `admin-analytics.ts` `dataLayer` pushes for `admin_*` events (see runbook). | No change required if dimensions for `discount_type`, `discount_amount`, `error_type`, and `section_id` already exist for booking discount events. | Add/verify GTM triggers for auto-apply events if needed; optional separate admin container for `admin_referral_qr_*` / `admin_instance_uuid_copied`. | Updated `analytics-ga4-gtm-runbook.md` (event catalog + admin section); this change log. |
| 2026-04-12 | `cursor/analytics-form-outcome-standard-9bdd` (fix) | Hardened `trackPublicFormOutcome` merge order; `trackAnalyticsEvent` client early-return; booking attempt discount params only when discounted; runbook trigger wording; tests for `validation_error` and analytics guards. | No change. | No change. | Runbook + tests only. |
| 2026-04-12 | `cursor/analytics-form-outcome-standard-9bdd` | Form analytics: added `trackPublicFormOutcome()` in `analytics.ts` (merges `form_kind` + optional `form_id` into dataLayer). Taxonomy adds `media_form_submit_attempt`, `community_signup_submit_attempt`, `booking_submit_attempt`; form events require `form_kind` and allow `form_id`. Contact, media, community, and booking reservation forms use the helper; new attempt events and `validation_error` where client validation fails. `validate-analytics-contract.mjs` validates `trackPublicFormOutcome` and treats static `formKind` as supplying `form_kind`; governance script treats `trackPublicFormOutcome` as analytics-impacting. | Register event-scoped custom dimensions `form_kind` and `form_id` if not already present (automation script updated). No key event renames. | No trigger renames; ensure new events (`*_submit_attempt`) are mapped if tags should fire on them. Variables reading `booking_submit_error` params should treat empty string as valid (see runbook). | Updated `analytics-ga4-gtm-runbook.md` (empty-string contract for `booking_submit_error`, new events, `form_kind`/`form_id`). Updated `ga4-create-custom-dimensions.py` display names for new params. |
| 2026-03-20 | `cursor/google-meta-ads-performance-c69e` | Added `trackEcommerceEvent()` to `analytics.ts`. Fire GA4 standard eCommerce events (`begin_checkout`, `add_payment_info`, `purchase`) alongside existing custom booking events in `my-best-auntie-booking.tsx`, `reservation-form.tsx`, `events.tsx`, and `landing-page-booking-cta-action.tsx`. No new custom events added to taxonomy. | Created 3 remarketing audiences via Admin API: Course Page Visitors (90d), Booking Intent - Did Not Complete (30d), High-Engagement Visitors (90d). No custom dimension/key event changes. | GTM eCommerce tags needed: add custom-event triggers + GA4 event tags for `begin_checkout`, `add_payment_info`, `purchase` with "Send Ecommerce data" enabled. | Updated `analytics-ga4-gtm-runbook.md` with eCommerce event catalog section and GTM setup guidance. Added `marketing/reports/ga4-manual-setup-steps.md`. |
| 2026-03-20 | `cursor/thank-you-modal-cards-f80f` | Booking thank-you modal: multi-session `.ics` (`buildBookingIcsCalendarContent`), text link CTA, and `booking_thank_you_ics_download` tracking (taxonomy + `trackAnalyticsEvent`). | Confirm `booking_thank_you_ics_download` + params in GA4 if registered. | Confirm GTM mapping for `booking_thank_you_ics_download` if used. | Updated `docs/architecture/analytics-ga4-gtm-runbook.md` and `docs/architecture/marketing-stack.md`. |
| 2026-03-20 | `cursor/thank-you-modal-content-a5ea` | Booking thank-you modal: removed `booking_receipt_print_click`; added `booking_thank_you_ics_download` in `apps/public_www/src/lib/analytics-taxonomy.json` and `trackAnalyticsEvent` on calendar (.ics) download in `thank-you-modal.tsx`. | Add/verify GA4 event + params `cohort_date`, `total_amount`. Retire `booking_receipt_print_click` in GA4 if it was registered. | Add/verify GTM custom-event trigger + GA4 tag for `booking_thank_you_ics_download`; remove print-event mapping if present. | Updated `docs/architecture/analytics-ga4-gtm-runbook.md` and `docs/architecture/marketing-stack.md`. |
| 2026-03-18 | `cursor/landing-page-organization-plan-506a` | Added `landing_page_cta_click` to `apps/public_www/src/lib/analytics-taxonomy.json` and instrumented landing-page CTA tracking in the new reusable landing-page flow. | Add/verify event-scoped custom dimension `landing_page_slug`. No new key events required. | Add/verify GTM custom-event trigger + GA4 event tag mapping for `landing_page_cta_click` with `landing_page_slug`, `age_group`, and `cohort_label` params. | Updated `docs/architecture/analytics-ga4-gtm-runbook.md` event catalog and GA4/GTM setup guidance for landing-page analytics. |
| 2026-03-14 | `cursor/ga4-api-access-verification-f10c` | Added shared `trackAnalyticsEvent` instrumentation for contact/media/community/booking/WhatsApp flows. | Created event-scoped custom dimensions, custom metrics (`total_amount`, `discount_amount`), and key events (`contact_form_submit_success`, `media_form_submit_success`, `community_signup_submit_success`, `booking_submit_success`). | Enabled GTM API automation; created workspace `Cursor GA4 taxonomy v1` with DLVs, custom-event triggers, and GA4 event tags for taxonomy events (draft not auto-published). | Added `docs/architecture/analytics-ga4-gtm-runbook.md`, linked from `overview.md`, and aligned GTM host-gating docs. |
| 2026-03-16 | `cursor/website-indexing-improvements-77fe` | SEO content optimization (trailing slash fix, sitemap hreflang alternates, robots.txt disallow rules, keyword-aligned meta copy, course-specific FAQ). | Data retention updated from 2 months to 14 months. Google Signals confirmed enabled. GA4 linked to Google Ads (499-114-4901) and Search Console. | Published GTM v6 (custom events live), v7 (page view dedup fix), v8 (tag reference routing), v9 (Google Ads AW-18019068393 conversion tag added). Removed duplicate GA4 Page View tag. Fixed dual-destination issue by separating rogue G-H4P7G413SZ tag. | Added `docs/architecture/marketing-stack.md` documenting full GCP/GTM/GA4/Ads/social stack. |
| 2026-03-16 | `cursor/system-user-instagram-scopes-ca01` | Added Meta Pixel integration (`init-meta-pixel.js`, `MetaPixel` component, `trackMetaPixelEvent` helper). Added `links_hub_click` event to taxonomy. Wired Meta Pixel standard events (`Lead`, `Schedule`, `InitiateCheckout`, `Contact`, `ViewContent`) alongside existing GA4 events in conversion components. Added link-in-bio page (`/links`) with click tracking. | No GA4 changes required; `links_hub_click` and Meta Pixel events are additive. A GTM custom-event trigger + GA4 event tag for `links_hub_click` can be added when needed. | No GTM container changes. Meta Pixel loads independently via `init-meta-pixel.js` (not through GTM). | Updated `analytics-ga4-gtm-runbook.md` event catalog. Updated `marketing-stack.md` with Meta Pixel docs and Meta Business account details. |

