# Cursor rules for Evolve Sprouts repository
# Sources:
# - Internal repo architecture/docs
# - Flutter, Python, React/TypeScript, and Next.js guidance

## Scope and applicability (MANDATORY)

- These rules apply to ALL executions in this repository, including:
  analysis, planning, implementation, refactors, tests, docs, and git ops.
- Use path-aware rules:
  - If a change touches `backend/**`, backend + security + docs rules apply.
  - If a change touches `apps/evolvesprouts_app/**`, Flutter rules apply.
  - If a change touches `apps/admin_web/**`, `apps/public_www/**`, or
    `apps/training/**`, web rules apply.
  - If a change touches multiple areas, satisfy all relevant sections.
- If two rules conflict, follow the more specific rule for the changed path.

## Repository structure (current)

repo root
|-- apps/
|   |-- evolvesprouts_app/    # Flutter mobile app
|   |-- admin_web/            # Next.js admin app
|   |-- public_www/           # Next.js public website
|   `-- training/             # Next.js training site (polls, workshops)
|-- backend/
|   |-- db/                   # Alembic + seed SQL
|   |-- infrastructure/       # AWS CDK stacks (TypeScript)
|   |-- lambda/               # Lambda handlers
|   |-- src/app/              # Shared Python app code
|   `-- requirements.txt
|-- docs/
|   |-- api/                  # OpenAPI specs
|   `-- architecture/         # Architecture docs
|-- scripts/
`-- tests/

## Constraints (MANDATORY)

- Never proceed with new files if similar functionality already exists.
- If the approved plan changes, re-submit the updated plan for approval.
- Use simple future tense in plans:
  - Good: "I will modify..."
  - Avoid: "I am modifying..."
- S3 bucket names must be 63 characters or less.
- Never hardcode secrets, API keys, passwords, or tokens.
- For CDK-created AWS Secrets Manager resources (`new secretsmanager.Secret(...)`)
  in `backend/infrastructure/**`, always set an explicit `secretName`.
- `secretName` must use the format `${resourcePrefix}-<kebab-case-suffix>`.
- The suffix must be lowercase letters/numbers with hyphens (`-`) as separators
  (for example `openrouter-api-secret`), with no spaces, underscores, or camelCase.
- Categorically ban hardcoded environment-specific values (for example domains,
  origins, base URLs, emails, ARNs, account IDs, region-specific endpoints, and
  deployment/runtime configuration). Derive them from parameters, environment
  variables, or repository config sources.
- Allowed exceptions for hardcoding: standards/protocol constants, local-only
  `localhost` development values, non-behavioral test/docs examples using
  `example.com`, and the fixed AR invoice PDF footer clause **"Proudly registered in Hong Kong"**
  (product jurisdiction line chosen under remediation Option B; not deployment-specific configuration).

## Workflow (MANDATORY)

1. Analyze the user request and relevant repository context.
2. Plan in chat before editing code. The plan must include:
   - files to modify/create
   - functions/components/modules to change
   - edge cases and risks
   - questions to validate assumptions
3. Wait for explicit user approval.
4. Execute only after approval.
   - Treat all write operations as implementation and blocked before approval.
   - Implementation includes file create/edit/delete, dependency changes,
     migrations, generated artifacts, and git write operations
     (`git add`, `git commit`, `git push`).
   - Read-only analysis steps (search, file reads, planning) are allowed before
     approval.
5. If implementation scope changes after approval, stop and request renewed
   explicit user approval.
6. Validate with focused checks/tests for touched areas.
7. For any Python change, run:
   `pre-commit run ruff-format --all-files`
   before committing.
8. Keep the repository AI-rule contract validation passing:
   `bash scripts/validate-cursorrules.sh`

## Architecture decisions (repository baseline)

- Admin web app uses Next.js App Router under `apps/admin_web/src/app`.
- Public website uses Next.js static export in `apps/public_www`.
- Infrastructure as Code uses AWS CDK (TypeScript) in
  `backend/infrastructure`.
- Lambda handlers are under `backend/lambda/**` and shared backend code is
  under `backend/src/app/**`.
- Aurora PostgreSQL schema is managed by Alembic in `backend/db`.
- In-VPC Lambda database access is through RDS Proxy with IAM auth.
- In-VPC Lambda calls to Cognito/external HTTP must go through
  `app.services.aws_proxy.invoke()` or `http_invoke()`.
- API endpoint contracts are documented only in `docs/api/*.yaml`.

## Backend and API rules (MANDATORY for backend changes)

- Register new API routes in:
  `backend/infrastructure/lib/api-stack.ts`
- Keep API Gateway route paths exactly aligned with Lambda handlers.
- For every new or changed endpoint:
  1. Update API Gateway route wiring in CDK.
  2. Update OpenAPI in `docs/api/admin.yaml` or `docs/api/public.yaml`.
  3. Update Lambda catalog in `docs/architecture/lambdas.md`.
- Do not merge endpoint work without all three updates.
- Do not call boto3 `cognito-idp` directly from in-VPC Lambdas.
  Use the AWS proxy service helpers instead.
- Asset API module layout is mandatory:
  - Place asset route handlers and helpers under
    `backend/src/app/api/assets/**`.
  - Use `*_assets.py` for route modules and `assets_*.py` for shared
    helpers (`assets_common.py` request parsing, `assets_storage.py` S3 and
    CloudFront link helpers, `assets_serializers.py` ORM serializers).
  - Keep `backend/src/app/api/admin.py` focused on request dispatch orchestration.
### CDK Lambda physical names (`backend/infrastructure/**`)

- Stack helpers set physical Lambda names to `${resourcePrefix}-${constructId}` (CloudWatch log groups follow `/aws/lambda/${functionName}`). **Do not** use construct ids that repeat the product or `resourcePrefix` token (for example `EvolvesproutsFooFunction` when `resourcePrefix` is `evolvesprouts`), or the deployed name becomes redundant (`evolvesprouts-EvolvesproutsFooFunction`). Prefer a single descriptive PascalCase id without a second product prefix (for example `ImportLegacyVenuesFunction`, `AwsApiProxyFunction`).
- Renaming a Lambda construct id replaces the CloudFormation resource; update `docs/architecture/lambdas.md`, `docs/architecture/aws-assets-map.md`, and any workflow or IAM examples that reference the physical name.

## Database change rules (MANDATORY for schema changes)

- Alembic revision IDs must be <= 32 characters.
  - Format: `NNNN_short_description`
  - Example: `0054_add_audit_log`
  - Validate length with:
    `echo -n "revision_id" | wc -c`
- Seed-data assessment is required for EVERY DB change.
  Seed file: `backend/db/seed/seed_data.sql`
- Checklist (must answer all):
  1. Compatibility with existing seed SQL.
  2. New NOT NULL/CHECK-constrained columns handled in seed data.
  3. Renamed/dropped columns reflected in seed data.
  4. New tables evaluated for seed rows.
  5. Enum/allowed-value changes validated in seed rows.
  6. FK/cascade changes validated for insert order and references.
- Confirm migration + seed sequence mentally or in practice:
  `alembic upgrade head` then `psql -f seed_data.sql`
- If no seed update is needed, add a brief commit/PR note explaining why.

## Documentation freshness (MANDATORY after code changes)

- Keep relevant docs updated with code changes:
  - `docs/api/*.yaml` for endpoint/schema/auth changes
  - `docs/architecture/lambdas.md` for Lambda inventory/behavior changes
  - `docs/architecture/database-schema.md` for table/column changes
  - `docs/architecture/aws-assets-map.md` for AWS/IAM/CDK output changes
  - `docs/architecture/overview.md` for high-level architecture changes
  - `docs/architecture/decisions.md` for architectural pattern changes
  - `docs/architecture/security.md` for security model changes
  - `docs/architecture/aws-messaging.md` for SNS/SQS pattern changes
  - `docs/architecture/audit-logging.md` for audit behavior changes
  - `.cursorrules` when project conventions change
- Architecture docs describe design and decisions.
  Endpoint specifics belong only in OpenAPI specs.

## Web testing requirements (MANDATORY for web behavior changes)

- Applies to `apps/admin_web`, `apps/public_www`, and `apps/training`.
- When UI/API changes alter web behavior, update existing tests and fixtures
  in the app's current testing framework.
- If coverage is missing, add focused tests in the app's existing toolchain.
- Do not require Playwright unless it is already in use for the touched app
  or explicitly requested.
- Keep mock responses aligned with `docs/api/*.yaml` and current UI behavior.

## Security requirements (MANDATORY)

- Never log unmasked PII.
  Use `mask_email()` and `mask_pii()` from `app.utils.logging`.
- Never use `print()` in production code.
  Use structured logging with `get_logger()`.
- Never use `random` for security tokens; use `secrets`.
- Never use `Cors.ALL_ORIGINS` in production.
- Always use `noEcho: true` for secret CDK parameters.
- Always use fail-closed attestation in production
  (`ATTESTATION_FAIL_CLOSED=true`).
- Always validate and sanitize user input.
- Always enforce SSL for DB connections (`sslmode=require`).

## General code style

- Write concise, readable, and maintainable code.
- Avoid duplication; prefer reusable components/functions.
- Use descriptive names (for example, `isLoading`, `hasError`).
- Keep Python files under 500 lines.

## Flutter rules (apply to `apps/evolvesprouts_app/**`)

- Use MVVM-style separation where practical:
  - `lib/models/`
  - `lib/viewmodels/`
  - `lib/views/widgets/`
  - `lib/views/screens/`
  - `lib/services/`
  - `lib/utils/`
- Prefer `StatelessWidget` when state is not needed.
- Use `const` constructors for immutable widgets.
- Use Riverpod for state management patterns in this app.
- Use Material widgets and consistent `ThemeData`.
- Naming:
  - files/directories: snake_case
  - classes/types: UpperCamelCase
  - variables/methods: lowerCamelCase
- Import order:
  1. `dart:*`
  2. third-party `package:*`
  3. project-relative imports

## Next.js and TypeScript rules

### Applies to `apps/admin_web/**`, `apps/public_www/**`, and `apps/training/**`

- Use TypeScript for app code.
- Prefer functional components and declarative composition.
- Prefer server components by default; minimize `use client`.
- Use `src/app` for routes and `src/components` for shared components.
- Favor named exports for reusable components.
- Use descriptive, intention-revealing names.
- Keep Tailwind usage consistent and mobile-first.
- For simple pure helpers, use the `function` keyword.
- Styling must live in CSS classes/files, not inline style objects.
- Never use `style={...}` or `style=""` in JSX (MANDATORY).
- Never use `CSSProperties` / `React.CSSProperties` in app code (MANDATORY).

### SVG icons (MANDATORY for `apps/admin_web/**` and `apps/public_www/**`)

- Do not embed inline `<svg>...</svg>` markup in feature, section, page, or
  shared UI components (excluding third-party or generated sources you must not
  edit).

**`apps/admin_web/**`**

- Store vectors in `src/components/icons/svg/` (kebab-case); import via SVGR
  (`@svgr/webpack` in Next webpack; use `next dev --webpack` / `next build --webpack`
  in package scripts). Vitest uses `vite-plugin-svgr` with `include: '**/*.svg'`.
- Thin wrappers may merge `className` / `aria-*` / `data-*` onto the SVGR import
  only—no `<path>` / `<circle>` / `<g>` in TSX.
- Canvas rasterization use-cases (for example QR code logo compositing) may
  reference assets from `apps/admin_web/public/` since SVGR imports are not
  trivially rasterizable.

**`apps/public_www/**`**

- Store reusable UI vectors in `public/images/*.svg` (same tree as other static
  SVG assets). Reference them with `/images/...` from `<img>` or from CSS
  `mask-image` (see `es-ui-icon-mask*` in `components-core.css`). Do not keep
  parallel copies under `src/components/icons/`.
- Wrapper spans may set sizing and `currentColor` via Tailwind/CSS; do not inline
  vector markup in TSX.

### Admin Web OpenAPI typing contract (MANDATORY for `apps/admin_web/**`)

- Admin web API types for admin endpoints must be generated from
  `docs/api/admin.yaml`.
- Generated admin web types must live at
  `apps/admin_web/src/types/generated/admin-api.generated.ts`.
- Do not hand-edit generated types.
- When `docs/api/admin.yaml` or admin-web API typing changes, run:
  - `npm run generate:admin-api-types`
  - `npm run check:admin-api-types`
- Keep `npm run lint` enforcing the admin API type drift check.

### Admin Web API client modules (MANDATORY for `apps/admin_web/**`)

- All admin API calls go through `adminApiRequest` in
  `apps/admin_web/src/lib/api-admin-client.ts`; responses are read directly
  (the backend emits no `{ data }` envelope).
- Build list endpoint paths with `buildAdminListPath` from
  `apps/admin_web/src/lib/admin-list-query.ts`; do not hand-roll
  `URLSearchParams` builders in `lib/*-api.ts`. The shared page size is
  `ADMIN_LIST_PAGE_SIZE` (mirrors backend `DEFAULT_LIST_LIMIT`) and the cap is
  `ADMIN_API_MAX_LIST_LIMIT` (mirrors `MAX_LIST_LIMIT`); keep them in sync.
- One `lib/<resource>-api.ts` module per backend resource. View-model adapters
  (for example vendors over organisations) wrap the resource module instead of
  duplicating request code.

### Admin Web CRUD UX pattern (MANDATORY for `apps/admin_web/**`)

The admin product standard is **table-first, expand-in-place**. Every admin
CRUD screen with a list/table view (contacts, families, organisations, leads,
services, enrollments, invoices, payments, discount codes, assets, tags, and
so on) follows the same shape, built from the shared primitives in
`apps/admin_web/src/components/ui/`:

- **Page order:** filters, then the table. The table is the top of the page
  and has **no card title**; the section name already appears in the nav and
  page header. Do not render a listing card title or description.
- **Filters** (`AdminFilterBar` + `AdminFilterField` from
  `admin-filter-bar.tsx`) sit directly above the table with their labels above
  the controls. The **create** control (`AdminCreateButton`, a `+` icon button
  from `admin-create-button.tsx`) lives in the filter bar's trailing slot.
- **Rows expand in place** (`AdminRecordTable` + `AdminExpandableRow` from
  `admin-record-table.tsx` / `admin-expandable-row.tsx`). Clicking a row opens
  an animated detail region directly beneath that row containing the editor.
  Exactly one row is open at a time, driven by `useExpandedRecord`
  (`apps/admin_web/src/hooks/use-expanded-record.ts`), which syncs the open
  record id to a URL query parameter and prompts before discarding unsaved
  edits when switching rows.
- **Create** inserts a draft row (`DRAFT_RECORD_ID`) at the top of the table
  with its editor already open; saving replaces the draft with the created
  record, cancelling removes it.
- **Editors** (`AdminEditorPanel` + `AdminEditorActions` from
  `admin-editor-panel.tsx`) have **no title or subtitle**: field labels and
  fields only, then one action row (`justify-start`, `gap-2`). **Create** shows
  only the primary button. **Update** shows **Cancel** (secondary) to the
  **left** of the primary save action. When the field area is a `<form>`, give
  it a stable `id` and attach the submit control with `type='submit'` and
  `form='<id>'` so the primary button can live in the actions row without
  nesting forms invalidly.
- **Field layout** uses `AdminFieldGrid` + `AdminField` from
  `admin-field-grid.tsx` with `columns` of **1, 2, or 4** per row. Other
  column counts are not allowed; use `span` on an `AdminField` for a wider
  control inside a 2- or 4-column grid.
- **Sub-accordions** inside an editor (for example Location, Tags, Address)
  use `AdminDisclosure` from `admin-disclosure.tsx`.
- **Loading** renders `AdminSkeletonRows` (`admin-skeleton.tsx`) in the table
  body rather than a spinner or a blank card.
- Do not implement create/edit flows with modal, dialog, sheet, drawer, or a
  separate editor card above the table. `AdminDialog` remains for read-only
  detail viewers; `ConfirmDialog` remains for confirm/cancel decisions.
- **Exceptions** are allowed only where the data model forces them and must be
  documented in a code comment at the exception site (for example the contact
  phone number is a region + national number pair rendered as two controls
  inside one `AdminField`).
- Legacy screens that still render `AdminEditorCard` above `PaginatedTableCard`
  are migration debt; when touching such a screen for a behaviour change,
  migrate it to the primitives above instead of extending the legacy layout.

### Admin Web shell and layout standard (MANDATORY for `apps/admin_web/**`)

These rules are the product standard for admin UI consistency. Future changes
should extend the shared primitives rather than reintroducing one-off layouts.

- **Top-level navigation** (`ADMIN_NAV_ITEMS` and `ADMIN_NAV_DASHBOARD` in `apps/admin_web/src/lib/admin-nav.ts`, rendered by
  `apps/admin_web/src/components/admin-authenticated-shell.tsx`): **Dashboard** is a fixed leading entry (above a divider), then the primary areas listed alphabetically: **Assets**, **Audit**, **Calendar**, **Contacts**, **Finance**, **Sales**, **Services**, **Tags**, **Website**. The default
  section after sign-in remains **Finance** (including the root `/` redirect to `DEFAULT_ADMIN_SECTION_PATH`); Dashboard is available from the nav but is not the default landing route unless product requirements change.
- **In-area view switchers** (for example Finance, Services, Sales pipeline vs analytics)
  must use `AdminTabStrip` from `apps/admin_web/src/components/ui/admin-tab-strip.tsx`
  instead of ad-hoc button rows. It is implemented as a **button group** (`role="group"`,
  `aria-pressed` on the active control), not ARIA tabs; do not add `role="tablist"` /
  `role="tab"` without the full tabpanel pattern (panels, `aria-controls`, roving tabindex,
  arrow keys).
- **Listing blocks** follow the table-first CRUD pattern above
  (`AdminRecordTable` with `AdminFilterBar`). Table markup inside it uses
  `AdminDataTable`, `AdminDataTableHead`, and `AdminDataTableBody` from
  `apps/admin_web/src/components/ui/admin-data-table.tsx`, with a trailing
  **Operations** column (right-aligned header) for row actions.
  `PaginatedTableCard` and `AdminTableToolbar` are retained only for screens
  not yet migrated; do not introduce them on new screens.
- **Operations column controls** are rendered by `AdminRowActions`
  (`apps/admin_web/src/components/ui/admin-row-actions.tsx`) and are
  **icon-only**: every control is an `AdminIconButton` / `AdminIconLink`
  (`admin-icon-button.tsx`) of the **same size**, with a **border**, a
  **white background**, and a **tooltip** (`title` plus visually hidden label)
  naming the action. Never render text buttons, mixed sizes, or filled
  backgrounds in the Operations column. When a row has **more than two**
  actions, `AdminRowActions` keeps the first action inline and moves the rest
  into an overflow menu opened by a **three-dots** `AdminIconButton`; the menu
  is an anchored popover with keyboard navigation, not a full-screen modal.
  Destructive actions use the `danger` tone inside the menu and still confirm
  through `ConfirmDialog`.
- **Detail viewers and other non-confirm modals** use `AdminDialog` from
  `apps/admin_web/src/components/ui/admin-dialog.tsx`. `ConfirmDialog` is only
  for confirm/cancel decisions.
- **Server state is TanStack Query.** The single `QueryClient` comes from
  `getAdminQueryClient()` in `apps/admin_web/src/lib/admin-query-client.ts`
  (provided by `AdminQueryProvider` in the root layout) and every key is built
  from `adminQueryKeys` in `apps/admin_web/src/lib/admin-query-keys.ts`. Do
  not hand-roll `useEffect` + `useState` fetch loops, module-level caches, or
  ad-hoc key arrays in feature code. Tests call
  `resetAdminQueryClientForTests()` (already wired in `tests/setup.ts`).
- **Cursor-paginated lists** are driven by `usePaginatedList`
  (`apps/admin_web/src/hooks/use-paginated-list.ts`, an `useInfiniteQuery`
  wrapper); do not hand-roll cursor, debounce, or abort state in feature hooks.
  Fetchers used by a list must be exported module-level functions (for example
  `fetchAdminContactsPage`) so `prefetchPaginatedList` and the nav-intent
  prefetcher (`apps/admin_web/src/hooks/use-prefetch-admin-section.ts`) can
  reuse them.
- **Shared reference data** (entity tags, admin users, instructors, geographic
  areas, locations) is read through the `useShared*` hooks in
  `apps/admin_web/src/hooks/use-admin-catalog.ts` (backed by `useQuery`);
  invalidate through the exported `invalidateShared*` helpers after mutations
  instead of ad-hoc refetches.
- **Hooks live under `apps/admin_web/src/hooks/`**, not beside components.
- **Delete, void, and other row-scoped destructive or state-changing actions**
  belong in the table **Operations** column (with `ConfirmDialog` where
  appropriate), not in the editor footer. `ConfirmDialog` may render optional
  `children` between the description and the action buttons when extra input
  is required (for example a void reason).
- **Asset file replacement** (upload new binary for the selected asset) lives in
  the **inline editor** field area, not the list **Operations** column, because
  it depends on editor context (metadata, PDF validation, and the two-step
  presigned flow) rather than being a row-only table action.
- **Motion** uses the tokens in `apps/admin_web/src/app/globals.css`
  (`--duration-admin-expand`, `--ease-admin-expand`) through
  `AdminExpandRegion`; do not add per-component JS animation libraries or
  bespoke keyframes for expand/collapse.

### Public WWW XSS policy (MANDATORY for `apps/public_www/**`)

- Do not use `dangerouslySetInnerHTML`.
- Do not use `eval()` or `new Function()`.
- Keep `csp:inject` and `csp:validate` required in the public website build
  pipeline.
- During code review, reject changes introducing any of these patterns.

### Public WWW component and test structure (MANDATORY for `apps/public_www/**`)

- Place page composition components in `apps/public_www/src/components/pages/**`.
- Place reusable UI primitives/layout helpers in
  `apps/public_www/src/components/shared/**`.
- Place section components in `apps/public_www/src/components/sections/**`.
- Place section-only shared helpers in
  `apps/public_www/src/components/sections/shared/**`.
- Place all unit/component tests in `apps/public_www/tests/**`.
- Do not create `*.test.*` or `*.spec.*` files under
  `apps/public_www/src/**`.
- Mirror source domains in tests (`tests/components/**`, `tests/lib/**`,
  `tests/content/**`) to keep navigation predictable.

### Public WWW content-key naming convention (MANDATORY for `apps/public_www/**`)

- Treat `apps/public_www/src/content/*.json` key paths as the source of truth
  for section naming.
- Content key segments must use lowerCamelCase.
 - Required examples:
  - `aboutUs.hero`
  - `aboutUs.intro`
  - `aboutUs.myHistory`
  - `aboutUs.myJourney`
  - `aboutUs.whyUs`
  - `contactUs.form`
  - `contactUs.faq`
  - `myBestAuntie.hero`
  - `myBestAuntie.outline`
  - `myBestAuntie.booking`
  - `myBestAuntie.description`
  - `events.notification`
- Build section component names from content key paths using PascalCase.
 - Examples:
  - `content.aboutUs.whyUs` -> `AboutUsWhyUs`
  - `content.contactUs.faq` -> `ContactUsFaq`
- Build section file names from content key paths using kebab-case and place
  them under `apps/public_www/src/components/sections/**`.
 - Examples:
  - `AboutUsWhyUs` -> `about-us-why-us.tsx`
  - `ContactUsFaq` -> `contact-us-faq.tsx`
- `SectionShell` identifiers must match the section file key in kebab-case.
 - Set both `id` and `dataFigmaNode` to the same normalized value.
 - Stable in-page anchor ids may diverge from the file key when product navigation
   depends on them (for example `resources`, `intro-call-booking`); in those cases
   keep `id` and `dataFigmaNode` equal to the established anchor id.
 - Example:
  - `about-us-why-us.tsx` uses `id='about-us-why-us'` and
    `dataFigmaNode='about-us-why-us'`.
- Do not introduce or reuse legacy contact key names
  (`contactUs.contactUsForm`, `contactUs.contactFaq`).

### Public WWW family-consultations JSON key contract (MANDATORY for `apps/public_www/**`)

- This section applies ONLY to:
  - `apps/public_www/src/content/family-consultations.json`
- In this file, keys must use `snake_case`.
- Do not add or keep camelCase keys in this file.
- Location fields must use:
  - `location_name`
  - `location_address`
  - `location_url`
- Do not use legacy location keys:
  - `address`
  - `address_url`
  - `locationName`
  - `locationAddress`
  - `locationUrl`
- Keep other locale-content contracts unchanged (for example lowerCamelCase
  section paths in `en.json`, `zh-CN.json`, `zh-HK.json`).

### Public WWW locale content contract (MANDATORY for `apps/public_www/**`)

- Treat `apps/public_www/src/content/en.json` as the locale content schema and
  source of truth. Keep `zh-CN.json` and `zh-HK.json` aligned in the same
  change.
- All user-visible copy must live in locale JSON files. This includes headings,
  body copy, CTA labels, validation/error/success messages, placeholder labels,
  image alt text, `aria-label`, `aria-roledescription`, and other
  screen-reader-only text.
- Do not add new hardcoded user-visible English copy in
  `apps/public_www/src/app/**`, `apps/public_www/src/components/**`, or
  locale-aware helpers under `apps/public_www/src/lib/**`. Read it from locale
  content instead.
- Put shared cross-page UI copy under `common.*`. Keep section-specific copy in
  the owning section key (for example `testimonials.a11y.*` or
  `myBestAuntieBooking.paymentModal.*`).
- For locale strings that interpolate runtime values, use a `*Template` suffix
  and named placeholders such as `{title}`, `{label}`, `{author}`, or
  `{serviceTier}`.
- Prefer removing user-visible fallback literals from code once a locale key
  exists. If a fallback is still required, it must come from locale content,
  not a hardcoded string in app code.

## React/Redux guidance (when Redux is used)

- Use Redux Toolkit patterns (`createSlice`, `createAsyncThunk`).
- Use typed hooks for dispatch/selectors.
- Keep selectors in one place and reuse them.
- Add robust async error handling and user-safe error states.

## Python project guidance (apply to `backend/**` and `tests/**`)

- Keep modules focused and composable (models/services/controllers/utils).
- Use type hints and clear function signatures.
- Use robust error handling with contextual logging.
- Use pytest for tests.
- Keep docstrings and README/docs aligned with behavior.
- Use Ruff-based formatting/lint workflows configured by the repo.
